extends Camera2D
@onready var fish_guy = $"../.."


# Called when the node enters the scene tree for the first time.
func _ready():
	#print(fish_guy.stage)
	#limit_top = fish_guy.stage.camera_ybounds.x
	#limit_bottom = fish_guy.stage.camera_ybounds.y
	#limit_left = fish_guy.stage.camera_xbounds.x
	#limit_right = fish_guy.stage.camera_xbounds.y
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
